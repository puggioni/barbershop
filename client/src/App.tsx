import React from "react";
import "./App.css";

function App() {
  const handleClick = () => {};
  return (
    <div className="App">
      <button onClick={() => handleClick()}>Store</button>
    </div>
  );
}

export default App;
